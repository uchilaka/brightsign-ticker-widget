{
  "name": "brightsign-ticker",
  "version": "1.0.0",
  "description": "Ticker to show stock price in brightsign display",
  "main": "index.js",
  "license": "UNLICENSED",
  "private": false,
  "scripts": {
    "demo": "node-dev demo.js",
    "start": "json-server --watch ./build/ticker_data.json --port ${PORT:-8080}"
  },
  "dependencies": {
    "express": "^4.16.3",
    "json-server": "^0.14.0"
  },
  "engines": {
    "node": "~8.11"
  }
}
